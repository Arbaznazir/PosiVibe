service:
  name: posivibe-api
  type: web
  source:
    directory: api
  buildpacks:
    - name: nodejs
  build:
    command: npm install
  run:
    command: npm run start:prod
    workdir: /api
  ports:
    - port: 8800
