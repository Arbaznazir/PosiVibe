{
  "name": "posivibe-root",
  "version": "1.0.0",
  "description": "Root package for PosiVibe project",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd api && npm run start:prod",
    "start:prod": "cd api && node index.js",
    "build": "cd api && npm install"
  },
  "workspaces": [
    "api",
    "client"
  ]
}
